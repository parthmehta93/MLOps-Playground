apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: churn-predictor
  namespace: churn-model
spec:
  predictor
    serviceAccountName: sa-s3-access
    sklearn:
      storageUri: s3://mlops-mehtaparth93-churn-model-bucket-cicd/models/churn_model.pkl
      resources:
        requests:
          cpu: "500m"
          memory: "512Mi"
        limits:
          cpu: "1"
          memory: "1Gi"
